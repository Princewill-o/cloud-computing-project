import httpClient from "../../../services/httpClient";

export const dashboardService = {
  // This service can be expanded with dashboard-specific endpoints
  // For now, it's a placeholder for future dashboard-specific functionality
};

