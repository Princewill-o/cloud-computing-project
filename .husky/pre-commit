echo "🔍 Running pre-commit checks..."

cd frontend || exit 1

# Run linting (warn only, don't block)
echo "📝 Checking code style..."
npm run lint || echo "⚠️  Linting issues found - please review"

# Run type checking (warn only for now - missing deps)
echo "🔎 Type checking..."
npm run type-check || echo "⚠️  Type checking issues found - please review"

# Try to build (this is the critical check)
echo "🏗️  Building project..."
npm run build || {
  echo "❌ Build failed! Please fix build errors before committing."
  exit 1
}

echo "✅ All checks passed! Proceeding with commit..."
